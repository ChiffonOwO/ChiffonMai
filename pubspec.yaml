name: my_first_flutter_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.

# version: 1.12.2+35
version: 2.3.0+40

environment:
  sdk: '>=3.0.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_spinkit: ^5.2.0 # 美观的加载动画
  internet_connection_checker: ^1.0.0+1 # 检查网络连接
  google_fonts: ^6.3.0
  provider: ^6.1.5+1
    # 基础依赖：获取文件路径
  path_provider: ^2.1.2
  # 基础依赖：处理权限（Android 需申请存储权限）
  permission_handler: ^11.3.1
  # 基础依赖：处理字节数据
  http: ^1.6.0
  # 基础依赖：扫描文件并通知系统更新
  media_scanner: ^2.2.0
  # 图表库
  syncfusion_flutter_charts: ^20.1.61
  fl_chart: ^1.1.1
  archive: ^4.0.9
  marquee: ^2.3.0
  flutter_screenutil: ^5.2.0 # 屏幕适配
  shared_preferences: ^2.4.1 # 本地存储
  url_launcher: ^6.3.2
  # 网络图片缓存
  cached_network_image: ^3.3.1  
  flutter_cache_manager: ^3.3.1  
  # 音频播放
  just_audio: ^0.9.34
  # 随身听的后台播放 + 通知栏/锁屏控制。
  # 它内部就是 audio_service，负责 MediaSession、前台服务与通知栏按钮；
  # 它要求 Android 侧 Activity 继承 AudioServiceActivity（见 MainActivity.kt），
  # 并声明 com.ryanheise.audioservice.AudioService 服务（见 AndroidManifest.xml），
  # 三处缺一都会「能播但没有通知栏」。
  just_audio_background: ^0.0.1-beta.17
  audioplayers: ^6.5.1
  package_info_plus: ^9.0.1
  crypto: ^3.0.7
  markdown_widget: ^2.3.2+8
  web_socket_channel: ^3.0.3
  uuid: ^4.5.3
  pinyin: ^3.3.0
  kana_kit: ^2.1.1
  jp_transliterate: ^1.0.5
  simai_flutter: ^0.4.1
  redis: ^4.0.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  mysql1: ^0.20.0

  # flutter_soloud: ^3.4.10
  fluttertoast: ^8.2.4
  file_picker: ^11.0.2
  dio: ^5.9.2
  safe_text: ^2.1.5
  image_picker: ^1.1.2
  image: ^4.5.3
  mobile_scanner: ^7.2.0
  geolocator: ^14.0.2
  geocoding: ^5.0.0
  charset: ^2.0.1
  font_awesome_flutter: ^10.7.0  # 友链页 GitHub 图标
  # flutter_colorpicker 已移除：它的取色区宽度写死 300，在手机 AlertDialog
  # 里溢出、取不到色，设置页改成自绘的自适应取色器（见 SettingsPage 里的
  # _ColorPickerDialog / _ColorPickerArea）。
  http_parser: ^4.0.2  # multipart/form-data 媒体类型（ApiClient + OcrService）
  path: ^1.9.0  # 文件路径处理（SettingsPage 后台图片）

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  # 测试里 fake just_audio 的平台实现（见 test/portable_ball_interaction_test.dart）：
  # 悬浮球/迷你卡片要真的能起播才测得动，而真机以外没有原生插件
  just_audio_platform_interface: ^4.6.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

   # 图片资源配置（注意缩进：assets 是 flutter 的子项，前面有2个空格）
  assets:
    - assets/bg/
    - assets/cover/
    - assets/version/
    - assets/kaleidxscope/
    - assets/ratingbg/
    - assets/ratingnum/
    - assets/qrcode/
    - assets/background.png
    - assets/userinfobg.png
    - assets/userinfobg2.png
    - assets/chiffon2.png
    - assets/icon.png   # 关于页展示的 App 图标（原来是只给 flutter_launcher_icons 用的构建期素材，未打包）
    # dxrating 曲绘兜底的「songId → imageName」基线索引（构建期由 dxdata 生成，
    # 见 tool/gen_dxrating_cover_index.cjs）。dxdata 有 4MB、实测下载 200s+，
    # 所以必须随包带一份基线，运行时只在必要时尝试更新。
    - assets/dxrating_cover_index.json
    - assets/cover_manifest.json
    - assets/res/
    - assets/grade/
    - assets/gamrank/
    - assets/dxstars/
  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
flutter_launcher_icons:
  android: true  # 开启Android图标生成
  ios: true      # 开启iOS图标生成
  image_path: "assets/icon.png"  # 基础图标路径（2048×2048px）
  adaptive_icon_background: "#FFFFFF"      # Android 自适应图标背景色（可选）
  adaptive_icon_foreground: "assets/icon.png"  # Android 自适应图标前景（可选）
